# spring:
#   application:
#     name: fintech
# /src/main/resources/application.yml
spring:
  r2dbc:
    url: r2dbc:h2:mem:///fintechdb
    username: sa
    password:

  h2:
    console:
      enabled: true
  sql:
    init:
      mode: always
      schema-locations: classpath:schema.sql
logging:
  level:
    "org.springframework.r2dbc": DEBUG
